version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build -w @calc-app/web
  artifacts:
    baseDirectory: apps/web/dist
    files:
      - "**/*"
